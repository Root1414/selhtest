# Source Tree Analysis: Sellh (صله)

> **Generated by BMAD document-project workflow — 2026-02-24**

---

## Overview | نظرة عامة

This document describes the recommended/expected directory structure of the Sellh project and the purpose of each directory and file. The current state of the project (Phase 1) is a monolith; this analysis reflects both the current structure and the recommended structure for implementation.

---

## Current Project Root (As Found)

```
Sellh صله/                          ← Project root
│
├── docs/                            ← BMAD-generated knowledge base (this folder)
│   ├── project-scan-report.json     ← BMAD workflow state file
│   ├── index.md                     ← Master documentation index
│   ├── project-overview.md          ← Project summary
│   ├── architecture.md              ← System architecture
│   ├── source-tree-analysis.md      ← This document
│   ├── development-guide.md         ← Dev setup guide
│   ├── api-contracts.md             ← API endpoints
│   └── data-models.md               ← Database schema
│
├── _bmad/                           ← BMAD AI workflow framework
│   ├── bmm/                         ← Business Management Module
│   │   ├── config.yaml              ← Project configuration
│   │   └── workflows/               ← BMAD workflow definitions
│   ├── bmb/                         ← Business Module Builder
│   ├── core/                        ← BMAD core tasks
│   └── _config/                     ← Agent configurations
│
├── _bmad-output/                    ← BMAD workflow outputs
│   ├── planning-artifacts/          ← PRD, epics, stories
│   ├── implementation-artifacts/    ← Dev artifacts
│   ├── test-artifacts/              ← QA artifacts
│   └── bmb-creations/              ← Module builder outputs
│
├── Senior Project Week#6.pdf        ← Weekly progress report
└── مستند متطلبات الأعمال.docx      ← Business requirements document
```

---

## Recommended Project Source Structure | الهيكل المقترح للكود

```
Sellh صله/
│
├── app/                             ← Flask Application Root
│   ├── __init__.py                  ← App factory, Flask initialization
│   ├── config.py                    ← Configuration classes (Dev/Prod)
│   │
│   ├── models/                      ← Database models (SQLAlchemy or raw SQL)
│   │   ├── __init__.py
│   │   ├── user.py                  ← USERS table model
│   │   ├── factory.py               ← FACTORY table model
│   │   ├── local_product.py         ← LOCAL_PRODUCT table model
│   │   ├── procurement_item.py      ← PROCUREMENT_ITEM table model
│   │   └── ai_match_log.py          ← AI_MATCH_LOG table model
│   │
│   ├── routes/                      ← Flask Blueprint route handlers
│   │   ├── __init__.py
│   │   ├── auth.py                  ← POST /auth/login, POST /auth/logout
│   │   ├── factories.py             ← GET/POST/PUT /factories
│   │   ├── products.py              ← GET/POST /products
│   │   ├── procurement.py           ← POST /procurement/match
│   │   └── analytics.py             ← GET /analytics/local-content-score
│   │
│   ├── services/                    ← Business logic layer
│   │   ├── __init__.py
│   │   ├── ai_matching.py           ← Sentence-Transformers matching engine ⭐
│   │   ├── factory_service.py       ← Factory CRUD logic
│   │   ├── product_service.py       ← Product CRUD logic
│   │   └── analytics_service.py     ← Local content score calculation
│   │
│   ├── middleware/                  ← Flask middleware and decorators
│   │   ├── __init__.py
│   │   └── auth.py                  ← @require_admin, @require_buyer decorators
│   │
│   └── templates/                   ← Flask HTML templates (if server-side)
│       ├── base.html
│       ├── dashboard.html
│       └── ...
│
├── ai/                              ← AI Engine (can be its own microservice later)
│   ├── __init__.py
│   ├── embedder.py                  ← Sentence-Transformers model loading & embedding
│   ├── matcher.py                   ← Cosine similarity calculation & ranking
│   └── models/                      ← Downloaded AI model files (gitignored)
│
├── dashboard/                       ← Streamlit Dashboard (Phase 1 Frontend)
│   ├── main.py                      ← Streamlit entry point ⭐
│   ├── pages/
│   │   ├── home.py                  ← Home / overview page
│   │   ├── match.py                 ← Procurement matching page
│   │   └── analytics.py             ← Compliance analytics page
│   └── components/
│       ├── risk_indicator.py        ← Green/Yellow/Red traffic light
│       └── score_chart.py           ← Local content score chart
│
├── database/                        ← Database management
│   ├── schema.sql                   ← SQL schema creation script ⭐
│   ├── migrations/                  ← Schema migration scripts
│   └── seed_data.sql                ← Sample data for development
│
├── tests/                           ← Test suite
│   ├── test_ai_matching.py          ← AI matching accuracy tests
│   ├── test_api.py                  ← API endpoint tests
│   ├── test_models.py               ← Database model tests
│   └── conftest.py                  ← Pytest fixtures
│
├── main.py                          ← Flask application entry point ⭐
├── requirements.txt                 ← Python dependencies ⭐
├── .env.example                     ← Environment variables template
├── .gitignore                       ← Git ignore rules
└── README.md                        ← Project readme
```

---

## Critical Files & Entry Points | الملفات الحرجة ونقاط الدخول

| File | Purpose | Priority |
|------|---------|----------|
| `main.py` | Flask app entry point | ⭐ Critical |
| `app/__init__.py` | App factory function | ⭐ Critical |
| `app/services/ai_matching.py` | Core AI engine | ⭐ Critical |
| `database/schema.sql` | Database structure | ⭐ Critical |
| `dashboard/main.py` | Streamlit entry point | ⭐ Critical |
| `requirements.txt` | All Python dependencies | ⭐ Critical |
| `app/routes/procurement.py` | AI matching API | 🔸 High |
| `app/routes/analytics.py` | Compliance dashboard API | 🔸 High |

---

## Key Directory Purposes | أغراض المجلدات الرئيسية

| Directory | Purpose |
|-----------|---------|
| `app/models/` | SQLAlchemy ORM models or raw SQL query wrappers |
| `app/routes/` | Flask Blueprint route handlers (one file per feature area) |
| `app/services/` | Business logic, separated from HTTP layer |
| `ai/` | AI engine - isolated for future microservice extraction |
| `dashboard/` | Streamlit frontend code |
| `database/` | SQL schema, migrations, seed data |
| `tests/` | Test files using pytest |

---

## Integration Points | نقاط التكامل

```
dashboard/main.py
    └── Calls → Flask API (localhost:5000/api)
                    └── Uses → app/services/ai_matching.py
                                    └── Uses → ai/embedder.py + ai/matcher.py
                                                    └── Reads → LOCAL_PRODUCT (SQL DB)
```
